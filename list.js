let canvas;
let buttonOP;
function setup(){
canvas = createCanvas(windowWidth,windowHeight);
canvas.id("list");
buttonOP = createButton("Optical Path");
}

function draw(){
  clear();
  buttonOP.position(200,200);

}
